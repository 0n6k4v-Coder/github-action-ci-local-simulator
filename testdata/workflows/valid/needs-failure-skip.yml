name: needs-failure-skip

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo should be skipped
